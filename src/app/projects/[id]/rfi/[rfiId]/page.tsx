
'use client';

// This file is effectively replaced by /src/app/projects/[id]/communication/rfi/[rfiId]/page.tsx
// It can be deleted if no other routes depend on this specific path structure.
// For safety during refactoring, its content is cleared. If it's truly unused, it will be removed in a subsequent step.

export default function OldRfiDetailPage() {
  return null; // Or a redirect, or a message indicating the page has moved.
}
